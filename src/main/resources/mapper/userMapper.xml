<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kinaboot.muscles.dao.userMapper">
    <select id="count" resultType="int">
        SELECT count(*) FROM user
    </select>
    <select id="searchIdCnt" resultType="int">
        SELECT count(*) FROM user where id=#{id}
    </select>

    <select id="selectUser" resultType="UserDto">
        SELECT *
        FROM user
        where id = #{id}
    </select>

    <insert id="insertUser" parameterType="UserDto">
        INSERT INTO user
            (id,password,email,phone,birth,address,point,created_date)
        values
            (#{id},#{password},#{email},#{phone},now(),#{address},0,now())
    </insert>

    <delete id="deleteAllUser">
        DELETE FROM USER
    </delete>
</mapper>
